{
  "rules": {
    "users": {
      ".read": "true",
      ".write": "auth != null",
      "$uid": {
        ".read": "true",
        ".write": "$uid === auth.uid"
      }
    },
    "questions": {
      ".read": "true",
      ".write": " auth != null",
      "$qid": {
        ".read": "true",
        ".write": "auth != null && ( data.child('Uid').val() === auth.uid || !data.exists() )",
        ".validate": "newData.child('question').val().length > 0",
        "answers": {
          ".read": "true",
          ".write": "auth != null && ( data.child('Uid').val() === auth.uid || !data.exists() )"
        }
      }
    }
  }
}